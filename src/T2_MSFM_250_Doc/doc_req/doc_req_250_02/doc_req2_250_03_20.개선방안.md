# MSFM_T2_250 시스템 개선 방안 상세 설계 명세 (사용자 의사결정 반영 완료)

본 문서는 `doc_req2_250_03_11.이슈재정리.md`에 분류된 39개 시스템 이슈에 대하여 4대 분석 프레임워크를 기반으로 검토하고, **사용자의 최종 의사결정(권장 설계 방향 채택)을 반영하여 확정한** 상세 설계 명세입니다.

### 🛠️ 분석 프레임워크 4대 구분
1. **이슈 검토 결과 (Issue Validation):** 현 소스 코드(ArduinoJson V7 기준)와 설계 명세서 대비 정합성 검증 및 리스크 분석.
2. **개선 방안 (Proposed Resolution):** 문제 해결을 위한 구체적인 아키텍처 및 구현 기법 (C++/ESP-IDF 코드 포함).
3. **사용자 결정 완료 사항 (User Decision Adopted):** 개발자 의사결정이 완료되어 시스템에 최종 적용하기로 확정된 설계 및 정책 방향.
4. **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** ESP32-S3 하드웨어 리소스 한계 또는 소프트웨어 스택 복잡도에 따른 구현 난이도 분류 (매우 높음 / 높음 / 보통 / 낮음).

---

## 2. Phase 1: 크래시 방지 및 실시간성 패치 (최우선 반영)

### [이슈 1] GDMA 전송 및 CPU L1 캐시 일관성 (Cache Coherency) 제어 로직
* **이슈 검토 결과 (Issue Validation):**
  * GDMA가 PSRAM이나 외부 버퍼와 데이터 직접 전송 시 CPU L1 Cache의 복사본이 동기화되지 않아 데이터 오염이 발생할 수 있는 리스크가 매우 큽니다. `esp_cache_msync()` 호출 및 버퍼 배치 선언이 누락되었습니다.
* **개선 방안 (Proposed Resolution):**
  * 버퍼 할당 시 16/32바이트 정렬을 맞추고, DMA 수집 전후로 아래 API를 명시 호출하여 일관성을 확보합니다.
  ```cpp
  // DMA로 PSRAM에 데이터를 받기 전: 캐시 영역 무효화 (Invalidate)
  esp_cache_msync((void*)_psramFftWindow, FFT_SIZE * sizeof(float), ESP_CACHE_MSYNC_FLAG_INVALIDATE);
  // CPU가 PSRAM 데이터를 수정한 후 DMA로 보낼 때: Write-back 플러시 (Dirty)
  esp_cache_msync((void*)_psramFftWindow, FFT_SIZE * sizeof(float), ESP_CACHE_MSYNC_FLAG_DIRTY | ESP_CACHE_MSYNC_FLAG_UNALIGNED);
  ```
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * 캐시 동기화에 따른 미세 지연 트레이드오프보다 데이터 신뢰성이 절대적이므로, 예외 없이 전체 캐시 일관성 제어 로직을 활성화 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통 - ESP-IDF 표준 캐시 제어 API 적용으로 Feasible.

### [이슈 9] FSM 매니저 내부의 제로 카피(Zero-Copy) 원칙 위배 (memcpy 남발)
* **이슈 검토 결과 (Issue Validation):**
  * `T290_FsmMgr_250.cpp` 내부의 `_vibProcessTask`에서 `memcpy`로 임시 구조체에 데이터를 깊은 복사하여 DMA의 성능적 이점을 잠식하고 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 데이터 교환을 값 복사가 아닌 락 프리 큐 포인터 기반의 소유권 스왑 구조로 리팩토링합니다.
  ```cpp
  ST_FeatureSlot_Vib_t* v_slotPtr = nullptr;
  if (_ringBuf.dequeue(&v_slotPtr)) {
      _dsp.processAccel(v_slotPtr->raw_data); // 제로카피 접근
  }
  ```
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 힙 단편화와 동적 메모리 제어 지연을 방지하기 위해, 락프리 링버퍼는 **고정 정적 배열 기반의 인덱스 순환 방식**으로 수명 주기를 고정 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - 전체 파이프라인의 데이터 교환 아키텍처 리팩토링 공수 필요.

### [이슈 12] ESP-DSP FFT 복소수 버퍼 크기 불일치
* **이슈 검토 결과 (Issue Validation):**
  * `_sramFftPing[1024]`에 `dsps_fft2r_fc32_ae32`를 호출하면 복소수 연산(실수/허수 쌍, $2\times N$)으로 인해 1024개 범위를 넘어선 힙 파괴가 즉각 발생하여 치명적입니다.
* **개선 방안 (Proposed Resolution):**
  * 버퍼 크기를 2048 float 크기로 2배 증설하고 Interleaved 방식으로 전처리 배열을 채웁니다.
  ```cpp
  alignas(16) float _sramFftPing[2048]; // 1024-point 복소수 FFT용
  for (int i = 0; i < 1024; i++) {
      _sramFftPing[2 * i] = raw_input[i];     // Real
      _sramFftPing[2 * i + 1] = 0.0f;         // Imag
  }
  dsps_fft2r_fc32_ae32(_sramFftPing, 1024);
  ```
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음. (즉각 교정 적용)
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통 - 버퍼 구조 선언과 Interleaving 전처리 루프 구성으로 가능.

### [이슈 13] FreeRTOS ISR 컨텍스트 위반
* **이슈 검토 결과 (Issue Validation):**
  * 인터럽트인 `T200_handleTriggerISR()` 내에서 일반 뮤텍스/블로킹 동기식 명령 디스패치 함수를 호출하여 즉각적인 커널 크래시(패닉) 유발 위험이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * FSM에 비블로킹 전용 `dispatchCommandFromISR()`를 신설하고 내부에서 `xQueueSendFromISR` 및 `portYIELD_FROM_ISR`을 통해 명령을 전달합니다.
  ```cpp
  BaseType_t CL_T2_FsmManager::dispatchCommandFromISR(uint32_t p_cmd) {
      BaseType_t xHigherPriorityTaskWoken = pdFALSE;
      xQueueSendFromISR(_cmdQueue, &p_cmd, &xHigherPriorityTaskWoken);
      return xHigherPriorityTaskWoken;
  }
  ```
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음. (커널 안정성을 위해 즉시 적용 필수)
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통 - FreeRTOS 표준 ISR 통신 매크로 활용으로 용이.

### [이슈 15] 락 프리 링버퍼 오버라이트 시 메모리 오더링(Memory Ordering) 역전
* **이슈 검토 결과 (Issue Validation):**
  * `enqueue()`가 꽉 찼을 때 `_tail` 포인터를 무거운 데이터 복사 전에 먼저 변경하여 읽기 단과의 동시성 오염 버그가 숨어 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 데이터 쓰기 복사가 끝난 후에만 포인터를 갱신하고 메모리 배리어(`std::memory_order_release`)로 기계적 최적화 차단.
  ```cpp
  _buffer[head] = item; // 데이터 복사 완료
  std::atomic_thread_fence(std::memory_order_release); // 하드웨어 배리어
  _tail.store((tail + 1) % _capacity, std::memory_order_relaxed); // 그 후 tail 갱신
  ```
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통 - C++ 원자적 펜스 연산 적용.

### [이슈 16] 스토리지 I/O 뮤텍스 블로킹에 의한 실시간 파이프라인 붕괴
* **이슈 검토 결과 (Issue Validation):**
  * SD 카드의 물리 병목(최대 500ms 지연) 동안 동일한 `_lock`을 공유하는 실시간 데이터 수집 태스크까지 일시 락업(정지)되어 센서 버퍼가 유실되는 구조적 모순이 확인됩니다.
* **개선 방안 (Proposed Resolution):**
  * 락 획득 임계 구역(Critical Section)을 포인터 교환부로만 축소(Lock Decoupling)하고, 실제 Write 및 Flush 물리 IO는 뮤텍스를 쥐지 않은 백그라운드 태스크에서 비차단식 수행하도록 분리합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** SD_MMC 물리 플러시 지연을 극복하기 위해 백그라운드 SD 기록 전용 링버퍼의 크기를 **3~5초 분량(약 500KB~1MB)**으로 설정하여 임시 할당합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - 백그라운드 I/O 데몬 태스크 전면 이원화 및 락 생명주기 관리 필요.

### [이슈 33] 지연 쓰기(Lazy Write) 데몬 틱(Tick)의 고아화(Orphaned)
* **이슈 검토 결과 (Issue Validation):**
  * 설정 플래시 마모 예방용 Lazy Write `checkLazyWrite()` 함수가 어디에서도 호출되지 않아 RAM 설정 변경 후 전원 차단 시 영구 유실되는 결함이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * FSM 유지보수 태스크 또는 T200 루프 내에 1초 단위로 폴링 호출하는 데몬 시퀀스 브릿지를 통합합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음 - 타이머 틱/폴링 바인딩만으로 Feasible.

### [이슈 38] 코어 마이그레이션과 Task WDT 기아(Starvation) 현상
* **이슈 검토 결과 (Issue Validation):**
  * DSP 태스크에 코어 바인딩을 주지 않아 Core 0(네트워크/센서 ISR 전담)으로 스케줄링 시 시스템 정체와 Task WDT 리셋 패닉을 유발합니다.
* **개선 방안 (Proposed Resolution):**
  * `xTaskCreatePinnedToCore`로 DSP/ML 태스크를 Core 1에 고정시키고, 대량 연산 루프에 `esp_task_wdt_reset()` 및 `vTaskDelay`를 강제 삽입합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통 - RTOS 태스크 생성 설정 재조율로 Feasible.

---

## 3. Phase 2: 데이터 구조 일반화 및 최적화

### [이슈 4] Policy Traits 메타프로그래밍 구조체 불완전 (마스킹 제약 부족)
* **이슈 검토 결과 (Issue Validation):**
  * `T2_General` 네임스페이스 하단에 제어 상수들이 생략되어, 설계안에 기획된 도메인별 연산 Pruning(가지치기)이 런타임 분기 조건식으로 우회되고 있습니다.
* **개선 방안 (Proposed Resolution):**
  * `static constexpr bool` 상수들을 Traits 구조체에 온전히 구현하고 `if constexpr` 기법을 통해 컴파일 타임 최적화를 보장합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

### [이슈 5] 자이로(Gyro) 전용 초경량 IIR 메모리 구조 미반영
* **이슈 검토 결과 (Issue Validation):**
  * 자이로는 고주파 대역이 불필요함에도 가속도 필터 구조체와 동일한 거대 FIR 필터 상태 버퍼를 상속받아 메모리 힙 공간이 낭비되고 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 자이로 구조체(`ST_GyroDspRuntime`)의 FIR 필터 필드를 제거하고 초경량 IIR 필터 상태인 `_gyroIirState[3][4]`로 대체 적용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 8] SMEA_FLASH_RODATA 매크로의 실제 적용 누락 (SRAM 낭비)
* **이슈 검토 결과 (Issue Validation):**
  * 필터 계수들이 런타임 구조체 내 alignas(16) float 멤버 배열로 선언되어 있어, 구조체 힙 할당 시 불필요한 복사 및 내부 SRAM을 수 KB 낭비합니다.
* **개선 방안 (Proposed Resolution):**
  * Notch 및 HPF 고정 필터 계수 배열들을 멤버 변수에서 제거하고 `const alignas(16)`를 결합해 Flash ROM 영역(XIP)에 직접 정적 상수로 배치하고 참조합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 14] TinyML AI 텐서 정렬(Alignment) 누락 (SIMD 로드 페널티)
* **이슈 검토 결과 (Issue Validation):**
  * 외부 SPIRAM에 `heap_caps_malloc`으로 할당된 텐서 버퍼가 32비트 정렬만 보장되므로, S3의 SIMD 벡터 연산 구동 시 정렬되지 않은 메모리 로드로 인한 심각한 성능 지연을 보입니다.
* **개선 방안 (Proposed Resolution):**
  * `heap_caps_aligned_alloc(16, size, MALLOC_CAP_SPIRAM)`을 적용해 16바이트 정렬을 강제 보장합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 향후 도입될 텐서 가속 라이브러리의 공통 표준 호환성을 위해, 정렬 요구 사양은 **16바이트 정렬**로 강제 준수하여 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 17] JsonDocument 풀링 미반영 및 지역 변수 남발 (힙 단편화)
* **이슈 검토 결과 (Issue Validation):**
  * 함수 콜백마다 `JsonDocument v_doc` 지역 변수를 무차별적 동적 생성 및 소멸시켜 단시간 내 힙 메모리 파편화를 일으키고 장기 작동 시 OOM 크래시를 유발합니다.
* **개선 방안 (Proposed Resolution):**
  * CfgMgr 내에 공용 멤버 변수 `JsonDocument _docPool`을 도입하고, 사용 전후로 `clear()` 및 `shrinkToFit()`을 호출하는 정적 풀링 기법을 적용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 공용 `_docPool`의 멀티코어 충돌을 제어하기 위해, ConfigManager의 임계 접근부를 감싸는 최소한의 세마포어 뮤텍스 락킹 구조를 가볍게 배치하여 동시성을 격리합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 19] 비원자적 변수(Non-Atomic)에 대한 메모리 배리어 무효화
* **이슈 검토 결과 (Issue Validation):**
  * `_is_alarm_latched` 같은 공유 플래그가 단순 bool 형이라 컴파일러 GCC 레지스터 최적화에 의해 타 코어 및 인터럽트에서 변경 상태를 즉각 관측하지 못할 위험이 큽니다.
* **개선 방안 (Proposed Resolution):**
  * 상태 제어 플래그 변수를 `std::atomic<bool>` 또는 `volatile` 형태로 변경 선언하여 최적화 고착을 방지합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 30] Zero-Copy 바이너리 텔레메트리의 패킷 조립 브릿지 증발
* **이슈 검토 결과 (Issue Validation):**
  * 가속도, 자이로, 오디오 데이터 특징량을 합쳐 전송하는 패킷 규격(`ST_PktTelemetry_t`)은 선언되었으나, 정작 연산부에서 병합 및 송출하는 브릿지 로직이 누락되어 스트리밍 기능이 유실된 상태입니다.
* **개선 방안 (Proposed Resolution):**
  * FSM 루프 내에 분리 전달된 두 슬롯의 데이터를 `ST_PktTelemetry_t`에 복사 없이 팩킹하는 어댑터 변환 및 `broadcastBinary()` 연동 루틴을 추가합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 통신 리소스 보존과 모니터링 반응 속도의 효율적 합의를 위해, 바이너리 텔레메트리 패킷 송출 주기는 **100ms**로 확정 튜닝합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

---

## 4. Phase 3: 하드웨어 모듈 및 비동기 파이프라인 연동

### [이슈 2] SPI 트랜잭션 큐(Transaction Queue) 기반 비동기 스케줄링
* **이슈 검토 결과 (Issue Validation):**
  * SPI 통신이 동기식 blocking으로 처리되어 타 스레드가 대기하고, vTaskDelay(0)로 우회하고 있어 전송 주기에 불규칙한 지터가 생기고 실시간성이 저하됩니다.
* **개선 방안 (Proposed Resolution):**
  * SPI 전송 구조를 캡슐화한 `ST_SpiTransaction_t` 큐 엔진으로 리팩토링하고 백그라운드 SPI 비동기 인터럽트 구동 드라이버(`spi_device_queue_trans`)로 전면 개편합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 비동기 전송 시 발생하는 하드웨어 오류는 런타임에 즉시 개입하기 어렵기 때문에, 단순 오류 카운트를 증가시킨 후 연속 3회 실패 시 FSM에 디바이스 폴트(ERROR)를 던지는 단순 임계 판정형 콜백을 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - SPI 드라이버 전체 계층 및 큐 아키텍처 재설계 필요.

### [이슈 11] I2S DMA 디스크립터 경계와 제로 카피 스트라이드(Stride) 충돌
* **이슈 검토 결과 (Issue Validation):**
  * I2S DMA 청크와 FFT Hop Size가 일치하지 않아 2개의 DMA 버퍼에 걸친 데이터를 합성하기 위해 CPU가 매번 `memcpy`를 수행하는 오버헤드가 발생하고 있습니다.
* **개선 방안 (Proposed Resolution):**
  * `FFT_SIZE`와 `OVERLAP_RATIO`를 결합하여 `I2S_DMA_CHUNK_SIZE`를 컴파일 타임에 강제 도출하고, 이를 I2S 하드웨어 DMA 초기화 설정에 바인딩합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음. (수학적 결합을 통해 오버헤드를 원천 제거)
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 20] 핫스왑 설정 변경 후 백그라운드 데몬 동기화 누락
* **이슈 검토 결과 (Issue Validation):**
  * SSID나 IP, 브로커 주소가 핫스왑 저장되더라도 구동 중인 통신 데몬(`T270`)은 이 사실을 알 수 없어 하드 리부팅 전까지 기존 네트워크 소켓을 강제 유지하는 모순이 존재합니다.
* **개선 방안 (Proposed Resolution):**
  * 변경 감지 시 FSM에서 `CMD_RESTART_NETWORK` 명령을 하달하고, `T270`이 스스로 기존 소켓을 닫고 재기동하도록 런타임 시퀀스를 링킹합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 네트워크 재접속 시 발생하는 대량의 CPU 부하와 Wi-Fi 스택 경합으로 인한 신호 처리 왜곡을 방지하기 위해, 통신 데몬 재기동 중에는 **센서 데이터 수집 파이프라인을 일시 중지(Pause)**하는 단순화 차단 방식을 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 21] 사전 트리거(Pre-Trigger) 버퍼의 파일 덤프(Dump) 연동 누락
* **이슈 검토 결과 (Issue Validation):**
  * 설계 상 Pre-Trigger 링버퍼는 할당되었으나 정작 결함 감지 시 이를 파일 앞부분에 기록하는 오케스트레이션 인터페이스가 부재하여 중요 결함 전조 신호가 누락됩니다.
* **개선 방안 (Proposed Resolution):**
  * `CL_T2_Storage`에 `dumpPreTriggerToSession()` 함수를 구현하여, FSM이 결함 감지 세션을 오픈할 때 링버퍼 데이터부터 파일 상단에 강제 덤프 후 실시간 기록을 이어가도록 링키지를 설정합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[의사결정 완료 및 권장안 확정]** SRAM 고갈 방지를 위해 과거 저장 시간은 **최대 3~5초 범위**로 제한하고, 버퍼 메모리는 반드시 **PSRAM(MALLOC_CAP_SPIRAM)** 영역에 물리 할당하도록 정책을 고정합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 24] 모드 전환 시 하드웨어 FIFO 잔여 데이터 오염 (Flush 무결성 결함)
* **이슈 검토 결과 (Issue Validation):**
  * FSM의 감시 모드 전환 시 센서 FIFO를 비우는 기능이 없어, 이전 모드의 찌꺼기 잔여 샘플이 유입되어 최초 특징량 연산을 심각하게 왜곡시키는 무결성 결함이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 모드 전환 시 센서 하드웨어 레지스터를 호출하여 FIFO를 초기화하는 `flushHardwareFifo()` 함수를 신설하고 FSM이 상태 전환 시 호출을 제어하도록 구성합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 25] 세션 닫기 전 '보류 중인(Pending) 데이터' 소실 (Graceful Shutdown 결함)
* **이슈 검토 결과 (Issue Validation):**
  * 정지 명령 시 디스크 비동기 기록 큐에 유예되어 있는 최종 데이터의 보전 대기 루틴이 없어, 기록 완료 전에 파일 핸들이 소진 및 유실되는 결함이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * `closeSession()` 내부에서 비동기 큐(`isEmpty()`)가 다 비워질 때까지 세션 종료 프로세스를 세마포어로 지연시키는 Graceful Flush 동기화 구조를 적용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** SD 물리 장치 장애 시 무한 데드락에 빠지는 리스크를 원천 제거하기 위해, 비동기 큐 대기 시간의 최대 임계치(Timeout)는 **3초 한계**로 강제 고정하여 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 26] 캘리브레이션 프로파일의 실시간 반영 누락
* **이슈 검토 결과 (Issue Validation):**
  * `T280`에서 캘리브레이션 프로파일 갱신 후, 센서 드라이버(`T230`)의 오프셋 보정 변수에 실시간으로 값을 다시 로드하여 주입해 주는 흐름이 연동되어 있지 않습니다.
* **개선 방안 (Proposed Resolution):**
  * 저장 완료 핸들러에 `CMD_RELOAD_CALIBRATION` 명령을 FSM에 발급하고 센서 엔진이 즉시 파일을 재로드하여 오프셋을 갱신하도록 콜백을 연결합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 28] OTA 업데이트 중 I2S/SPI 캐시 미스 크래시
* **이슈 검토 결과 (Issue Validation):**
  * 무선 OTA 바이너리 플래싱 중 내부 버스와 플래시 쓰기 캐시가 완전히 잠깁니다. 이때 Core 1 of I2S DMA/SPI 인터럽트가 지속 구동하면 메모리 접근 캐시 미스로 ESP32가 즉각 리부팅되어 업데이트가 영구 실패합니다.
* **개선 방안 (Proposed Resolution):**
  * FSM이 `CMD_OTA_START` 수신 시 센서 DMA 인터럽트를 정지하고 SPI 드라이버를 물리적으로 `stop()` 및 `Pause` 시킨 뒤 플래시 쓰기를 실행하도록 크로스 인터페이스를 구현합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** OTA 작업 실패 시 복구 안정성을 확보하기 위해, 기본 탑재된 ESP32의 듀얼 부트 파티션 롤백 정책을 충족하도록 OTA API를 정합 유지합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - 하드웨어 인터럽트 제어 및 예외 복구 시퀀스 구현 공수 필요.

### [이슈 32] 동적 필터 계수(Coefficient) 업데이트 단절
* **이슈 검토 결과 (Issue Validation):**
  * 노치/HPF 설정 변경 시 구조체 메모리에는 반영되지만, 실제 DSP 연산 필터 계수 리빌드는 이루어지지 않아 재부팅 전에는 변경 설정이 적용되지 않습니다.
* **개선 방안 (Proposed Resolution):**
  * 설정 변경 시 즉각 `T240_DspEng`의 `recalculateFilters()` 인터페이스를 호출하여 런타임에 바이쿼드 계수 테이블을 리빌드하도록 링키지를 설정합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 계수 업데이트 순간의 신호 단차로 인한 필터 수렴 충격(Ringing) 오동작을 배제하기 위해, 계수 재연산 시점에는 **필터 상태 버퍼도 함께 0.0f로 강제 초기화(Flush)**하도록 로직을 일치시킵니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 34] 딥슬립 진입 전 센서 하드웨어 PMU 제어 누락
* **이슈 검토 결과 (Issue Validation):**
  * 초절전 깊은 잠(`esp_deep_sleep_start()`) 진입 직전 센서의 모션 인터럽트 라인 및 BMI270 전력PMU를 Wake-up 조건으로 매핑하지 않고 sleep하여 설비가 재동작해도 MCU가 수면에서 깨어나지 못하는 정지 리스크가 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 슬립 함수 직전 `CL_T2_SensorEngine`에 Any-Motion 인터럽트 및 모션 활성화 하드웨어 PMU 전력 레지스터 세팅 루틴(`prepareDeepSleepWakeup`)을 경유하여 핀 매핑 완료 후 수면 모드 진입.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 설비의 미세 진동으로 인한 오작동 슬립 깨어남을 필터링하기 위해, PMU의 최소 감도 조건은 **0.1g 임계치 이상 및 연속 20ms 유지** 사양으로 튜닝 정의합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 35] SD_MMC 물리적 파일 시스템 경합 (Bus Thrashing)
* **이슈 검토 결과 (Issue Validation):**
  * 캘리브레이션 분석 시의 대용량 파일 읽기와 실시간 쓰기 태스크가 단일 SD 카드 물리 매체를 병렬 경합하여 IO 병목 및 실시간 큐 오버플로우를 초래합니다.
* **개선 방안 (Proposed Resolution):**
  * FSM 단에 강력한 `State Lock`을 도입하여, MAINTENANCE(교정) 상태 진입 시 실시간 데이터 저장 세션은 안전하게 `closeSession` 시킨 후 캘리브레이션 IO를 개시하도록 상호 배제합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[의사결정 완료 및 권장안 확정]** 버스 붕괴를 유발하는 물리 경합을 차단하기 위해, 교정 상태 진입 즉시 실시간 쓰기 세션은 강제로 선점 셧다운(Close)되는 정적 잠금장치(State Lock)를 활성화합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

---

## 5. Phase 4: 예외 처리, 시간 동기화 및 MLOps 메타데이터 강화

### [이슈 3] WAL 드라이버의 백그라운드 플러시 (Background Flush) 로직
* **이슈 검토 결과 (Issue Validation):**
  * 빠른 커밋만 있고 raw 설정 데이터를 정규 LittleFS(`/sys/cfg_250.json`)로 정리 및 다음 섹터 사전 소거(`Erase`)를 진행하는 백그라운드 이관 시퀀스가 부재합니다.
* **개선 방안 (Proposed Resolution):**
  * 주기적 백그라운드 데몬 함수인 `flushToLittleFSAndEraseNext()`를 구현해 유휴 사이클 시 저널 데이터의 LittleFS 동기화 및 다음 raw 플래시 섹터 비동기 소거를 연동합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 10] WAL 플래시 수명 단축 (Write Amplification) 및 저널링 누락
* **이슈 검토 결과 (Issue Validation):**
  * 수백 바이트 변경 마다 4KB 섹터를 통째로 지우고 쓰는 구조로 인해, 잦은 설정 갱신(MLOps 피드백 등) 시 파티션 플래시 수명이 급격히 마모됩니다.
* **개선 방안 (Proposed Resolution):**
  * 4KB 섹터 영역에 Magic Header 및 Sequence ID를 가진 블록을 순차 기입(Append-only)하고 꽉 찼을 경우에만 소거 후 전이합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 저널링 플래시 레이아웃 구현 난이도에 따른 런타임 안정성 위험을 배제하기 위해, **지연 쓰기(Lazy Write) 임계 지연을 3~5초 범위로 보강하는 우회 대안**을 확정 적용하여 쓰기 횟수를 하향 평탄화합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - 플래시 물리 저널링 드라이버 레이아웃 설계 공수 필요.

### [이슈 18] Non-Monotonic 시간의 동기화 오염 (SNTP Time Jump)
* **이슈 검토 결과 (Issue Validation):**
  * 타임스탬프 계산에 NTP/RTC 시간을 사용하여 Wi-Fi 재동기화 시 시계열 타임스탬프가 앞뒤로 튀며(Time Jump) 멀티레이트 정렬 모듈이 데드락에 빠지는 논리 붕괴가 관측됩니다.
* **개선 방안 (Proposed Resolution):**
  * 정밀 시계열 분석 및 센서 융합용 타임라인에는 RTC 시각을 전면 배제하고, 하드웨어 단조 증가 시간인 `esp_timer_get_time()`만 일방 사용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 부팅 시각 1회에 한해 RTC 시각과의 오프셋 관계를 체킹 갱신하여, 파일 네이밍 시점에는 Monotonic Time 기준의 정확한 절대 시각 추적이 매칭되도록 헬퍼 모듈을 구성합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 22] I2S와 SPI 이종 클럭 간의 시간 편차 (Clock Drift) 누적
* **이슈 검토 결과 (Issue Validation):**
  * APLL 클럭(오디오)과 SPI 폴링 틱(진동) 간 클럭 드리프트가 장시간 구동 시 누적되어 `MultiRateTimeAligner` 정렬 허용 오차를 벗어나 텐서 바인더가 데드락에 빠집니다.
* **개선 방안 (Proposed Resolution):**
  * 오디오 샘플 누적을 Master Clock으로 세팅하고 진동 스트림 데이터를 가상 동기화하며 편차가 어긋날 시 가장 가까운 과거 프레임을 강제 보정/보간하는 방식을 사용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[의사결정 완료 및 권장안 확정]** 물리 인터럽트 설계를 보존하기 위해 하드웨어적 피드백 루프는 배제하며, 편차 임계 범위를 완화하고 오류 누적 시 프레임을 버려 재매칭하는 **소프트웨어 Drop & Shift 방식의 예외 회복 모듈**을 도입 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 매우 높음 - 정밀 시계열 데이터 이종 보간/보정 로직 구현.

### [이슈 23] SD 카드 I/O 에러 발생 시 FSM 예외 처리 블랙홀
* **이슈 검토 결과 (Issue Validation):**
  * SD 에러 발생 시 스토리지 모듈만 에러 플래그를 켤 뿐, 상위 FSM에 보고되지 않아 실질적으로 센서 수집 및 DSP는 정상 가동되는 것으로 위장되는 모순이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * FSM 메인 태스크 루프에 `_storage.hasIoError()` 검증을 바인딩하여 검출 즉시 FSM 상태를 `EM_SystemState_t::ERROR`로 변경하고 전면 적색 LED 알람 점멸 및 MQTT 에러 코드를 발행합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 27] MQTT LWT와 로컬 진단 결과의 동기화 결함
* **이슈 검토 결과 (Issue Validation):**
  * 시스템 패닉 크래시로 워치독이 작동해 장비가 셧다운되는 상황에서 직전 결함 상태를 알지 못한 채 오프라인이 되는 결함이 존재합니다.
* **개선 방안 (Proposed Resolution):**
  * 최종 진단 결과 및 크래시 파라미터를 `RTC_DATA_ATTR` 메모리 영역에 기입하고, 리부팅 완료 후 초기화 시 Watchdog 트리거 감지 시 해당 로그를 읽어 서버로 즉각 긴급 전송합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** SRAM 메모리 정합성을 위해, RTC 백업 영역에 보존할 메타데이터의 크기는 크래시 상태 플래그 및 요약 에러 지표 정보(256바이트 미만)로 제한 확정합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 29] 학습된 환경 노이즈 프로필의 휘발성 누락
* **이슈 검토 결과 (Issue Validation):**
  * 자가 학습된 노이즈 프로필이 RAM 휘발성 영역에만 존재하여 재부팅 시마다 프로필이 유실되고, 재학습 기간 동안 결함 진단 오경보가 다량 발생하는 결함이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 학습 완료 시점에 노이즈 바이너리 파일(`/sys/noise_profile.bin`)을 저장하고, 부팅 시점에 우선 로드하여 노이즈 연산에 갱신 적용합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 주변 노이즈의 불규칙적 변동에 유연하게 대응하기 위해, 노이즈 학습 프로필 강제 리셋 및 백엔드 저장소 파일 업데이트는 **FSM 상의 수동 유지보수 명령 트리거**에만 의존하도록 설계합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 31] 프리트리거(Pre-Trigger)의 '기준 시간축(T0)' 상실
* **이슈 검토 결과 (Issue Validation):**
  * 프리트리거 과거 버퍼 덤프 시 정확한 트리거 기준 시점 T0 정보가 유실되어 덤프 파일 내 정상/이상을 명확히 분리 및 MLOps 분석할 수 없는 결함이 있습니다.
* **개선 방안 (Proposed Resolution):**
  * 세션 파일 오픈 시 절대 타임스탬프 T0 인자를 추가 전달받아, 바이너리 덤프 파일 최상단 메타데이터에 T0 타임스탬프를 수록하여 동기화합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 36] 오버라이트(Overwrite)에 의한 위상 불연속성과 스펙트럼 붕괴
* **이슈 검토 결과 (Issue Validation):**
  * 버퍼 꽉 참 시 단순 1샘플 오버라이트를 실행하여 신호 파형 위상에 `Hard Step(단차)`을 유발하고 스펙트럼 누설로 인한 100% 허위 결함 트리거 오동작을 일으킵니다.
* **개선 방안 (Proposed Resolution):**
  * 단일 샘플 드롭을 차단하고 1024 샘플 청크(FFT 윈도우 단위)로 프레임을 통째로 탈락(`Frame Drop`)시킨 후 신호 히스토리를 리셋하여 위상 연속성을 보호합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 오버플로우로 인한 신호 유실은 심각한 현장 결함 전조이므로, 프레임 드롭 발생 빈도는 **실시간 웹 UI 모니터링 페이지 및 내부 텔레메트리 에러 지표**에 명시적으로 노출 적용하도록 확정합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 37] 비동기 NTP 지연과 MQTT TLS 핸드셰이크 경합 (데드락)
* **이슈 검토 결과 (Issue Validation):**
  * NTP 타임 동기화 완료 전 MQTT MQTTS(TLS) 접속 시도 시 내부 시각(1970년)으로 인해 TLS 인증서 기간 만료 거부 연결 실패 교착이 발생합니다.
* **개선 방안 (Proposed Resolution):**
  * NTP 타임 동기화 API를 구동한 후 `timeinfo.tm_year > 120`을 만족할 때까지 MQTT 연결 루틴의 진입을 지연시키고 유보합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 내부망 또는 외부 NTP 통신이 불가능한 전용 폐쇄망 구동 환경에 대응하기 위해, 시간 동기화 대기 시간의 최대 임계치(Timeout)는 **30초**로 구성하고, 초과 시 1970년 가상 시각 기준으로 로컬 수집 저장 동작을 강제 개시하도록 조율합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 39] 플래시 가비지 컬렉션에 의한 텐서 조립 타임아웃
* **이슈 검토 결과 (Issue Validation):**
  * 파일 쓰기 시 발생하는 불규칙한 SD 카드 가비지 컬렉션 지연이 동적 메모리 조립부를 블로킹하여 시계열 데이터 결합 타임아웃을 유발하는 연쇄 실패가 확인됩니다.
* **개선 방안 (Proposed Resolution):**
  * 파일 세션 오픈 시 파일 크기 증가에 따른 물리 할당 병목을 막기 위해, 사전에 고정 영역 크기(예: 10MB)를 선할당(`f_lseek` 등을 활용)하여 디스크 공간을 선점 확보함으로써 쓰기 속도를 안정화합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 일반적인 결함 이벤트 저장 시나리오의 최대 저장 규격을 방어하기 위해, 선할당 크기는 **세션당 10MB** 고정 스펙으로 강제 정책을 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음 - FATFS 물리 클러스터 사전 할당 및 관리 제어 필요.

### [이슈 41] STA/LTA의 '콜드 스타트(Cold Start)' 초기화 모순에 의한 오경보 폭주
* **이슈 검토 결과 (Issue Validation):**
  * 이미 설비 가동 중에 시스템이 재부팅되는 경우, 초기 LTA 히스토리가 0.0f로 시작하여 실제 현장의 배경 진동에 직면하는 순간 단기 평균(STA) 대비 폭발적인 상승으로 오인해 수많은 허위 결함 판정이 발생합니다.
* **개선 방안 (Proposed Resolution):**
  * 시스템 부팅 직후 FSM에 N초(예: 30초) 동안의 `WARM_UP` (안정화) 상태 단계를 신설하여 해당 시간 동안 LTA 버퍼에 배경 신호를 완전히 충전한 후에만 진단 판정 플래그를 정상 개시하도록 방어 처리합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):** 없음. (WARM_UP 30초 시퀀스 기본 적용)
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### [이슈 43] MLOps 데이터 파이프라인의 '레이블 모호성' 발생
* **이슈 검토 결과 (Issue Validation):**
  * 결함 진단 트리거로 스토리지에 데이터를 덤프할 때 파일 내부에 어떤 결함 인자(임계치 초과 지표, 특정 주파수 대역 등)로 이상 판정이 났는지 상세 정보(그라운드 트루스)가 기록되지 않아 후속 학습 분석에 활용하기 어려움이 존재합니다.
* **개선 방안 (Proposed Resolution):**
  * 이상 데이터 속성 구조체(`ST_TriggerReason_t`: 축 번호, 지표 명칭, 초과 비율, 실 측정치 포함)를 설계하고 세션 바이너리 파일 헤더에 직접 임베디드하여 직렬화 기록합니다.
* **사용자 결정 완료 사항 (User Decision Adopted):**
  * **[권장안 적용]** 엣지 시스템 내 데이터 직렬화 부하를 O(1) 수준으로 극소화하기 위해, 이상 사유 전달 헤더 규격은 가변 규격을 배제하고 **바이너리 고정 크기 구조체(ST_TriggerReason_t)** 형태로 바인딩하여 확정 적용합니다.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

---

## 3. 리팩토링 과제 상세 설계 및 개선 방안 (총 49개)

`doc_req2_250_03_11.이슈재정리.md` 문서의 1.2절에 도출된 4단계 리팩토링 과제 49개에 대한 상세 설계 및 개선 방안을 다음과 같이 기술합니다.

### 가. 1단계: 데이터 모델 및 상태 규격화 (Data Model & Config Normalization)

#### [리팩토링 대상 1] 도메인별 설정 구조체의 중복 (T215_Type)
* **이슈 검토 결과 (Issue Validation):** `ST_Accel_Config_t`, `ST_Gyro_Config_t`, `ST_Audio_Config_t` 내부에 통계 임계치 등이 중복 정의되어 유지보수성이 낮고 정렬 규칙 적용이 불완전함.
* **개선 방안 (Proposed Resolution):** 공통 요소를 추출하여 템플릿 `ST_CommonFeatureCfg_t<size_t CH, size_t BANDS>`로 추상화하고 정적 합성(Composition)을 도입함.
* **사용자 결정 완료 사항 (User Decision Adopted):** 가상 함수를 배제하여 vtable 삽입을 방지하고 바이너리 직렬화 규격(#pragma pack)과 16바이트 정렬을 유지함.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 2] JSON 파싱 및 직렬화 로직의 거대화 (T220_CfgMgr)
* **이슈 검토 결과 (Issue Validation):** `_applyJson()` 및 `save()` 함수 하나에서 3개 센서의 파싱과 배열 매핑 로직이 중복 반복되어 코드 복잡도 가중.
* **개선 방안 (Proposed Resolution):** 1D/2D 배열 매핑을 `parseBandConfig<CH, BANDS>` 템플릿으로 헬퍼화하고, 각 서브 구조체에 `deserialize(JsonObjectConst)`를 두어 책임을 위임.
* **사용자 결정 완료 사항 (User Decision Adopted):** CfgMgr의 단순화를 위해 파싱 로직을 모듈별 구조체 멤버로 이관함.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 3] 상수 정의(Def)와 타입(Type)의 결합도 이슈 (T210_Def)
* **이슈 검토 결과 (Issue Validation):** T210_Def에 메모리 Limit 상수가 도메인별로 묶여 있어 가변 채널 정의 시 연쇄적인 코드 변경 초래.
* **개선 방안 (Proposed Resolution):** Limit(상한)과 Default(초기값) 네임스페이스를 분리하고, 타입을 템플릿화하여 Limit을 템플릿 인자로 주입받게 함.
* **사용자 결정 완료 사항 (User Decision Adopted):** 타입을 상수에 독립적인 일반 템플릿으로 설계해 확장성을 확보함.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 4] 특징량 데이터 컨테이너(Slot)의 파편화 (T215_Type)
* **이슈 검토 결과 (Issue Validation):** 연산 결과를 저장하는 `ST_Slot_Accel_t` 등의 내부 지표들이 중복 구성되어 있으나 도메인마다 개별 구현됨.
* **개선 방안 (Proposed Resolution):** 연산 결과를 `ST_FeatureMetrics_t<size_t CH, size_t BANDS>` 템플릿 구조체로 통합함.
* **사용자 결정 완료 사항 (User Decision Adopted):** 설정(Config)과 결과(Metrics)를 1:1 대칭 템플릿으로 설계하여 룰 판정 시 제네릭 연산이 가능하게 함.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 5] JSON 파싱과 도메인 유효성 검증(Validation)의 결합 (T220_CfgMgr)
* **이슈 검토 결과 (Issue Validation):** JSON 로딩 중 비즈니스 규칙 유효성 검사가 CfgMgr 내에 혼재되어 SRP 위반.
* **개선 방안 (Proposed Resolution):** CfgMgr는 순수 데이터 바인딩만 담당하고, 로드 후 구조체의 `validate()`를 호출하여 범위 초과나 모순을 자체 교정하게 함.
* **사용자 결정 완료 사항 (User Decision Adopted):** 비즈니스 로직과 데이터 파싱 로직의 의존성 차단 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 6] 공장 초기화(resetToDefault) 로직의 비대화 및 휴먼 에러 위험
* **이슈 검토 결과 (Issue Validation):** 멤버 변수가 파편화되어 공장 초기화 시 수백 개 변수에 하드코딩으로 대입하는 로직 존재.
* **개선 방안 (Proposed Resolution):** C++11 Default Member Initializer(`= T2_Def::...`)를 적용하고, 초기화 시 임시 기본 객체를 덮어씌우는 방식으로 전환.
* **사용자 결정 완료 사항 (User Decision Adopted):** 대입 스왑 방식으로 공장 초기화를 간결화하고 누락 에러 차단.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 7] 락프리 링버퍼(T216_RingBuf)의 대용량 값 복사 오버헤드
* **이슈 검토 결과 (Issue Validation):** enqueue/dequeue가 대용량 타입(4KB raw audio 등)을 값으로 복사하여 핫루프 내 CPU 병목 유발.
* **개선 방안 (Proposed Resolution):** 생산자가 버퍼에서 빈 슬롯의 참조/포인터를 받아 memcpy 없이 직접 채우고 쓰기를 커밋하는 Zero-Copy API 도입.
* **사용자 결정 완료 사항 (User Decision Adopted):** ISR 수집과 DSP 가공 지연 최적화를 위해 Zero-Copy 링버퍼 구조 구현 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 8] 텔레메트리 네트워크 패킷(ST_PktTelemetry_t)의 파편화 연쇄
* **이슈 검토 결과 (Issue Validation):** 송출 패킷 내 멤버들이 개별 도메인 구조체 크기에 강결합되어 가변 대역폭 도입 시 패킷 정의도 변경해야 함.
* **개선 방안 (Proposed Resolution):** 패킷 구조 내에 공통 `ST_FeatureMetrics_t`를 합성하거나, Flat Tensor(Memory View) 추상화로 float array 일괄 송출로 전환.
* **사용자 결정 완료 사항 (User Decision Adopted):** Flat Tensor 방식의 단일 스트리밍 규격을 적용하여 통신 패킷 확장성 확보.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 9] WAL 드라이버와 CfgMgr의 강한 책임 결합
* **이슈 검토 결과 (Issue Validation):** CfgMgr 소스 내부에 물리 플래시 파티션을 지우고 쓰는 `CL_T2_WalDriver` 로직이 혼재되어 SRP 위반.
* **개선 방안 (Proposed Resolution):** WAL 쓰기 로직을 `T225_WalEngine`으로 완전히 분리하고, CfgMgr는 `IConfigStorage` 추상 인터페이스를 사용하도록 격리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 저장 매체 백엔드 추상화 적용 및 DI 설계 채택.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 10] 템플릿 도입에 따른 메모리 패딩(Padding) 자동화 문제
* **이슈 검토 결과 (Issue Validation):** 구조체를 템플릿화하면 가변 크기로 인해 수동 패딩 멤버 지정이 어려워져 16바이트 정렬 규칙 붕괴 위험.
* **개선 방안 (Proposed Resolution):** C++ 메타 프로그래밍을 활용하여 컴파일 타임에 `uint8_t _pad_end[(16 - (sizeof(Payload) % 16)) % 16];` 패딩 크기를 자동 계산하도록 설계.
* **사용자 결정 완료 사항 (User Decision Adopted):** SIMD 연산 보장을 위한 컴파일 타임 패딩 식 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 11] JSON 키(Key) 문자열 파편화 및 플래시 ROM 낭비
* **이슈 검토 결과 (Issue Validation):** v_acc["rms_thresh"] 등 파싱 키 문자열들이 중복 하드코딩되어 rodata 영역을 낭비하고 오타 리스크 잔존.
* **개선 방안 (Proposed Resolution):** `T210_Def` 내에 `JsonKey` 네임스페이스를 두고 `constexpr const char*`로 통합하여 중앙 집중식 관리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 오타 에러 및 플래시 낭비 방지를 위해 딕셔너리 정적 상수화 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 12] 원시 파형 전송 패킷(ST_PktWaveform_t)의 중복
* **이슈 검토 결과 (Issue Validation):** 사고 파형 송출용 구조체가 도메인별로 이름만 다르고 동일하게 복사되어 3개 존재.
* **개선 방안 (Proposed Resolution):** `template <size_t MAX_SAMPLES> struct ST_PktWaveform_t` 단일 템플릿으로 통합하여 통신 계층의 인터페이스 단일화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 중복 바이너리 패킷 최소화.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 13] 플래시 WAL 바이너리 저장 방식의 OTA 하위 호환성 파괴
* **이슈 검토 결과 (Issue Validation):** 설정 구조체를 통째로 메모리 덤프해 플래시에 써서, 펌웨어 버전 업으로 구조 변경 시 데이터 뒤틀림 크래시 발생.
* **개선 방안 (Proposed Resolution):** `ST_WalHeader_t`에 스키마 버전을 삽입하고 버전 불일치 시 기본 JSON 설정 백업본(LittleFS)에서 안전하게 복구 및 마이그레이션.
* **사용자 결정 완료 사항 (User Decision Adopted):** 바이너리 포맷 스키마 버저닝 및 JSON 마이그레이션 로직 의무 탑재.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 14] 거대 설정 구조체(ST_DynamicConfig_t)의 멀티코어 Data Race와 RCU 부재
* **이슈 검토 결과 (Issue Validation):** Mutex Lock 하에서 변경되는 설정을 Core 1의 실시간 DSP 루프가 주기적으로 접근 시 블로킹 및 Data Race 유발.
* **개선 방안 (Proposed Resolution):** RCU(Read-Copy-Update) 및 더블 버퍼링 기법을 적용하여 DSP 태스크는 포인터만 스왑된 읽기 전용 버퍼를 읽도록 설계.
* **사용자 결정 완료 사항 (User Decision Adopted):** 실시간 핫루프 보호를 위해 포인터 원자적 교환(std::atomic) 구조 채택.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 15] Boolean 배열로 인한 캐시 라인 낭비 및 복사 오버헤드
* **이슈 검토 결과 (Issue Validation):** band_en[8] 등 참/거짓 판단을 위해 1바이트 bool 배열을 대량 정의하여 패딩 발생 및 캐시 효율 하락.
* **개선 방안 (Proposed Resolution):** 템플릿 일반화 시 bool 배열 대신 비트 필드(Bitfield) 또는 `std::bitset`으로 압축하여 메모리 크기 축소.
* **사용자 결정 완료 사항 (User Decision Adopted):** 메모리 사용량 압축 및 L1 캐시 히트율 극대화 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### 나. 2단계: 파이프라인 스테이지 추상화 (Pipeline Stage Abstraction)

#### [리팩토링 대상 16] DSP 연산 루프의 하드코딩 및 채널/축 종속성 제거 (T240_DspEng)
* **이슈 검토 결과 (Issue Validation):** processAudio(L/R), processAccel(X/Y/Z) 등 도메인별 축 개수가 하드코딩되어 필터 적용 순서 변경 시 중복 수정 발생.
* **개선 방안 (Proposed Resolution):** 물리적 축 명칭을 배제하고, N개의 독립된 1D 배열을 처리하는 범용 `_processSingleChannel()`을 순회하는 구조로 추상화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 가변 채널 지원을 위해 DSP 엔진을 1D 채널 기반 순환 구조로 재구축.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 17] 통계 특징량(RMS, 첨도 등) 추출의 도메인 분리 (T245_FeatExtra)
* **이슈 검토 결과 (Issue Validation):** 센서 도메인마다 RMS, 첨도 등의 특징 추출 루프가 각각 독립된 소스 코드로 복사되어 있어 유지보수성 저하.
* **개선 방안 (Proposed Resolution):** 수학적 추출 공식만 따로 모아 `MathUtil` 순수 함수 유틸리티로 격리하고, 내부에 ESP32-S3 SIMD 가속 적용.
* **사용자 결정 완료 사항 (User Decision Adopted):** SIMD 최적화 지점을 단일 유틸 모듈로 집중화하고 도메인별 연산을 일원화함.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 18] 켑스트럼(Mel-Filterbank) 생성 로직의 파라미터화 (T246_MelGen)
* **이슈 검토 결과 (Issue Validation):** 오디오용과 IMU용 멜 생성 루틴이 나뉘어 있고 밴드 수 및 FFT 크기가 하드코딩되어 있음.
* **개선 방안 (Proposed Resolution):** 도메인 구분을 없애고 `generateMelFilterbank(sample_rate, f_min, f_max, bands, fft_size)`로 완전 매개변수화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 범용 필터뱅크 엔진 설계를 확정 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 19] ESP32-S3 SIMD 가속 오버헤드 방어 (인라인 및 캐시 웜업)
* **이슈 검토 결과 (Issue Validation):** 함수를 쪼개어 추상화하면 루프 내 스택/가상 함수 오버헤드가 누적되어 실시간 처리 주기(12ms) 위반 가능성 존재.
* **개선 방안 (Proposed Resolution):** C++ virtual 다형성을 배제하고 inline 및 템플릿 기반 정적 다형성을 사용하며, DSP 함수들을 `IRAM_ATTR`로 선언해 캐시 미스 방지.
* **사용자 결정 완료 사항 (User Decision Adopted):** IRAM 상주 정책 및 C++ 템플릿 기반 정적 다형성 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 20] 추론 텐서 조립기(DynamicTensorBinder)의 하드코딩 모순 (T248_SeqBuild)
* **이슈 검토 결과 (Issue Validation):** 오디오와 진동 특징 조립 시 AUD_FEAT_CNT 등 하드코딩 오프셋 매크로를 사용하여 가변 채널 추상화와 모순.
* **개선 방안 (Proposed Resolution):** 각 특징량 슬롯이 스스로의 데이터 크기와 오프셋을 설명하는 'Shape Descriptor'를 내장하고 Binder는 이를 통해 동적 조립하게 분리.
* **사용자 결정 완료 사항 (User Decision Adopted):** Shape Descriptor 메타데이터 바인딩 방식 채택.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 21] 필터 상태(History/State) 객체의 채널 종속성 (T240_DspEng)
* **이슈 검토 결과 (Issue Validation):** Notch, IIR HPF 상태 구조체가 2채널(L/R) 고정 배열 크기로 되어 있어 다채널 확장 시 수동으로 뜯어고쳐야 함.
* **개선 방안 (Proposed Resolution):** 필터 계수와 상태 메모리를 바인딩해 `template <size_t CH> struct FilterState_t` 범용 컨테이너로 분리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 필터 상태 가변 채널 템플릿화 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 22] 우선순위 선점(Preemption)에 의한 FPU/Scratchpad 오염 위험 (T245, T40)
* **이슈 검토 결과 (Issue Validation):** Core 1에서 우선순위가 다른 오디오/진동 DSP 루틴 기동 시, 선점 인터럽트가 발생하면 수학 라이브러리 내부 static 공유 버퍼 및 FPU 레지스터 오염 리스크 존재.
* **개선 방안 (Proposed Resolution):** 모든 수학 유틸 내 static 변수 선언을 철저히 금지하여 100% 재진입성(Reentrancy)을 보장하고, FFT 작업 버퍼는 각 태스크별로 독립 메모리 영역을 할당 및 주입받아 사용.
* **사용자 결정 완료 사항 (User Decision Adopted):** FPU 선점 오염을 원천 방어하기 위해 공유 static 메모리를 사용하지 않는 재진입성 규정 강제.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 23] 동적 메모리 할당(heap_caps_malloc)에 의한 힙 단편화 시한폭탄 (T245_FeatExtra)
* **이슈 검토 결과 (Issue Validation):** 초기화 시점에 FFT, Mel 버퍼를 힙 메모리에서 동적 할당하여 캘리브레이션/재설정 반복 시 힙 단편화로 인한 OOM 유발 위험이 매우 큼.
* **개선 방안 (Proposed Resolution):** 최대 요구 사양(FFT_SIZE_MAX)에 맞춘 '정적 DSP 메모리 아레나'를 부팅 시점에 1회 크게 선할당하고, 각 모듈은 Placement New를 통해 해당 오프셋 주소만 분배받도록 제한.
* **사용자 결정 완료 사항 (User Decision Adopted):** 런타임 heap_caps_malloc을 전면 금지하고 정적 메모리 풀을 도입하여 OOM 원천 차단.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 24] MultiRateTimeAligner의 영구적 ZOH(Zero-Order Hold) 환각 현상 (T248_SeqBuild)
* **이슈 검토 결과 (Issue Validation):** SPI 통신 에러 등으로 진동 수집이 영구 정지되었을 때, 시간 정렬기가 마지막 정상값을 ZOH로 계속 합성하여 MLOps 모델에 공급함으로써 설비 결함 오판 유발.
* **개선 방안 (Proposed Resolution):** 데이터의 수명(Time-To-Live, TTL) 개념을 정렬기에 도입하여 일정 시간(예: 1초) 내에 갱신되지 않으면 강제로 NaN/0.0f 처리하고 `is_valid = false` 플래그로 차단.
* **사용자 결정 완료 사항 (User Decision Adopted):** 데이터 TTL 및 유효성 전파 마스킹 설계 도입.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 25] 윈도우 슬라이딩 및 델타 이력 버퍼의 memcpy 떡칠 제거 (T245_FeatExtra)
* **이슈 검토 결과 (Issue Validation):** 50% Overlap 및 델타 계수 윈도우 이동 시 memcpy와 수동 인덱스 연산이 남발되어 가독성을 저해하고 버퍼 오버런 위험 유발.
* **개선 방안 (Proposed Resolution):** 이력 이동 및 윈도우 팝업을 캡슐화한 `SlidingWindowBuffer<T, WindowSize, HopSize>` 템플릿 유틸리티를 구현하여 수학 연산부와 버퍼 관리부를 철저히 격리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 슬라이딩 윈도우 버퍼 템플릿 도입 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 26] 윈도우 계수(Window Coefficients) 배열의 독립 할당에 따른 SRAM 낭비
* **이슈 검토 결과 (Issue Validation):** 채널마다 FFT 윈도우 창 함수 가중치 배열이 개별 할당되어 SRAM 공간이 무의미하게 중복 낭비됨.
* **개선 방안 (Proposed Resolution):** 윈도우 계수 배열을 `.rodata` 영역의 정적 상수로 상주시키거나 싱글톤 공유 메모리로 추출하여 포인터만 주입받도록 개편.
* **사용자 결정 완료 사항 (User Decision Adopted):** 중복 SRAM 계수 테이블 배제를 통한 메모리 세이빙 정책 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 27] 재귀 필터 지연선(Delay Line)의 상태 찌꺼기(Artifact) 잔류 및 발산 위험
* **이슈 검토 결과 (Issue Validation):** FSM 상태 전이 시 IIR/Notch 필터 지연선 버퍼에 이전 가동 데이터 찌꺼기가 잔류하여 필터 기동 직후 발산(NaN) 또는 원치 않는 링잉 유발 위험.
* **개선 방안 (Proposed Resolution):** 필터 상태 컨테이너에 `flushFilterStates()` 인터페이스를 도입하고, 수집 개시/재시작 단계에서 모든 내부 상태 버퍼를 0으로 원자적 초기화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 수집 재기동 시 필터 클리어 시퀀스 의무 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 28] 노이즈 프로필 차감 시 수학적 예외(NaN/Inf) 발생 사각지대
* **이슈 검토 결과 (Issue Validation):** 배경 노이즈 프로필 차감 시 위상 소거로 전력이 음수(-)로 유입되면 후속 MFCC Log 연산 진입 시 예외 발생 및 SIMD 붕괴 리스크 존재.
* **개선 방안 (Proposed Resolution):** 차감 직후 음수 값을 차단하고 하한치를 규정하는 Spectral Floor 마스킹 연산 단계를 SIMD 벡터 루프와 병합해 안전성을 캡슐화.
* **사용자 결정 완료 사항 (User Decision Adopted):** Spectral Floor 마스킹 처리를 필수화하여 수치 연산 무결성 확보.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

### 다. 3단계: 모듈별 책임 분리 및 인터페이스 명확화 (Responsibility & Decoupling)

#### [리팩토링 대상 29] 스토리지 엔진(T260_Storage)의 도메인 종속성 제거
* **이슈 검토 결과 (Issue Validation):** `pushAudioFrame`, `pushVibFrame` 등 특정 센서 도메인에 결합된 인터페이스와 개별 파일 포인터가 존재하여 신규 센서 추가 시 스토리지 모듈 전면 수정 불가피.
* **개선 방안 (Proposed Resolution):** 센서 구분을 제거하고 '가상 스트림 채널' 아키텍처를 도입하여 `pushFrame(channel_id, payload, size)` 형태로 저장 인터페이스를 일원화 및 동적 채널 파일 핸들 매핑 구조로 전환.
* **사용자 결정 완료 사항 (User Decision Adopted):** 스토리지의 단일 I/O 전담화 및 센서 비종속 스트림 구조 구현 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 30] FSM 오케스트레이터(T290_FsmMgr)의 거대화(God Object) 방지
* **이슈 검토 결과 (Issue Validation):** FSM 매니저가 시스템 상태 제어뿐 아니라 태스크 방출 브로드캐스트와 하위 모듈 호출을 직접 통제하여 모듈 간 강결합 형성.
* **개선 방안 (Proposed Resolution):** 내부 이벤트 버스(Event Bus)를 신설하고 FSM은 오직 상태 전이 이벤트만 발행(Publish)하며, 수집/연산/저장 등 하위 모듈은 이를 구독(Subscribe)해 자체 동작하게 제어 역전(IoC) 패턴 적용.
* **사용자 결정 완료 사항 (User Decision Adopted):** 결합도 감소를 위해 펍섭(Pub-Sub) 기반 이벤트 전파 구조 도입.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 31] 판정 엔진(T250_Trigger)의 룰(Rule) 검사 하드코딩 해소
* **이슈 검토 결과 (Issue Validation):** `_checkAccelRules` 등 도메인별 검사 함수가 분리 작성되어 있고 설정 구조체 필드명에 하드코딩으로 접근하고 있어 유연성 저하.
* **개선 방안 (Proposed Resolution):** 템플릿 기반 결과 구조인 `ST_FeatureMetrics_t`에 맞춰 지표 배열의 정적 룰 평가기(Rule Evaluator)를 도입해 단일 연산 루프로 통합.
* **사용자 결정 완료 사항 (User Decision Adopted):** 도메인별 하드코딩 분기를 제거하고 룰 평가기 클래스로 통일.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 32] 비상 차단 인터록(Safety Interlock)의 호출 결합도 절연
* **이슈 검토 결과 (Issue Validation):** 오디오 연산 태스크(AudProcTask) 이상 판정 시 릴레이 차단 제어기인 `SafetyLifecycleManager`를 직접 호출하여 도메인 간 SRP 위반 및 결합도 형성.
* **개선 방안 (Proposed Resolution):** 판정 엔진은 결과(DetectionResult)만 반환하고, 부팅 시 주입받은 비상 차단 콜백(Emergency Callback) 포인터를 FSM 단에서 호출하게 설계 분리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 콜백 의존성 주입(DI) 방식을 채택하여 하드웨어 제어부와 연산부를 완전 절연.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 33] 스토리지 로테이션(File Rotation) 시 I/O 블로킹 및 큐 오버플로우 위험 (T260_Storage)
* **이슈 검토 결과 (Issue Validation):** 용량 한계 도달로 물리 파일 생성 및 스캔 시 수백 ms 동안 StorageTask 루프가 점유되어 비동기 수집 큐 오버플로우(데이터 손실) 발생.
* **개선 방안 (Proposed Resolution):** 파일 크기가 95% 도달 시 백그라운드(유휴 사이클)에서 다음 세션 저장 공간을 미리 선할당(Pre-allocation)하고 파일 핸들만 원자적으로 교환하도록 설계.
* **사용자 결정 완료 사항 (User Decision Adopted):** 파일 사전 점유/할당 스위칭 기법 도입.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 34] AI/MLOps 추론 엔진 확장을 배제한 판정 로직 구조 (T250_Trigger)
* **이슈 검토 결과 (Issue Validation):** 임계치 OR 룰 검사에만 엔진이 강결합되어 향후 TinyML 모델 탑재 시 if (use_ml) 등의 지저분한 분기 코드 대량 유입 리스크 존재.
* **개선 방안 (Proposed Resolution):** 판정 로직을 `IDiagnosticStrategy` 가상 인터페이스로 추상화하고, 기존 임계치 방식은 `RuleBasedStrategy`로 격리하며 ML 방식은 `TinyMLStrategy`로 구현하는 전략 패턴(Strategy Pattern) 적용.
* **사용자 결정 완료 사항 (User Decision Adopted):** 런타임 설정 주입 방식의 진단 전략 패턴 채택 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 35] 논리적 상태 머신과 RTOS 태스크 생명주기의 강한 결합 (T290_FsmMgr)
* **이슈 검토 결과 (Issue Validation):** FSM 매니저가 RTOS API(xTaskCreate, Suspend)를 직접 제어함으로써 태스크 오작동 발생 시 국소 스레드 복구가 불가능하고 전체 시스템 리셋에만 의존하게 됨.
* **개선 방안 (Proposed Resolution):** RTOS 태스크 할당 및 워치독 모니터링을 전담하는 `SystemTaskCoordinator`를 분리하고, FSM은 순수 상태 변경 신호만 이벤트 송출하도록 역할 분리.
* **사용자 결정 완료 사항 (User Decision Adopted):** RTOS 스레드 제어권 분리를 위해 SystemTaskCoordinator 신설 도입.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 36] 커맨드 디스패처(Command Dispatcher)의 스레드 안전성 및 ISR 컨텍스트 충돌 (T290_FsmMgr)
* **이슈 검토 결과 (Issue Validation):** 버튼 ISR, 네트워크 수신 콜백 등 다양한 스레드 컨텍스트에서 `dispatchCommand()`를 동시 직접 호출하여 상태 변경 Race Condition 발생 위험.
* **개선 방안 (Proposed Resolution):** 수신부와 처리부를 물리 격리하기 위해 스레드 안전한 Command Queue를 두고, FSM 매니저 메인 루프 안에서 큐를 순차 폴링하여 상태 전이를 동기식으로만 수행하게 수정.
* **사용자 결정 완료 사항 (User Decision Adopted):** 비동기 큐 기반의 FSM 명령 디스패처 직렬화 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 37] 네트워크 브로드캐스팅 I/O에 의한 실시간 FSM 루프 블로킹 (T270_Commu)
* **이슈 검토 결과 (Issue Validation):** 데이터 융합 즉시 FSM 매니저가 동기식으로 `_broadcastStreams` 웹소켓 바이너리 기입을 유발하여 WiFi 핑 튐 발생 시 수집 핫루프가 락업(정지)됨.
* **개선 방안 (Proposed Resolution):** 통신 모듈(T270) 내부에 네트워크 전용 락프리 송출 링버퍼를 분리 배치하고, 실제 TCP I/O는 최하위 우선순위의 백그라운드 네트워크 데몬 스레드가 전담 소화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 실시간 핫루프 보호를 위한 네트워크 송출 비동기 링버퍼 분리 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 38] 파일 시스템(FS) 동시 접근 경합 및 우선순위 역전 (T260_Storage, T280_Calibrator)
* **이슈 검토 결과 (Issue Validation):** 캘리브레이터가 설정/보정 파일 접근 시, 실시간 기록을 담당하는 스토리지 엔진과 물리 SD 버스 락(`_fsLock`)을 두고 경합하여 데이터 찢어짐(Tearing) 유발.
* **개선 방안 (Proposed Resolution):** 파일 시스템 접근 경로를 일원화하는 단일 'I/O 브로커 태스크'를 신설하고 모든 작업 메시지를 큐로 수신하여 중요도(High/Low)에 맞춰 동적 조율 및 직렬화.
* **사용자 결정 완료 사항 (User Decision Adopted):** 파일 시스템 충돌 차단을 위해 단일 I/O 브로커 아키텍처 도입.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 39] 판정 엔진(T250_Trigger)의 원인 추적(Root Cause Analysis) 메타데이터 상실
* **이슈 검토 결과 (Issue Validation):** 판정 엔진의 리턴값이 단순 Boolean/Enum이라 이상 감지 시 위반 채널, 지표, 수치 등 근본 원인 분석 컨텍스트가 유실되어 스토리지 파일로 보존되지 못함.
* **개선 방안 (Proposed Resolution):** 위반 인자 상세 정보(위반 채널, 지표 타입, 임계치 초과비율, 측정치)를 담은 `ST_DiagnosticReport_t`로 반환값 확장 및 블랙박스 파일 헤더 저장.
* **사용자 결정 완료 사항 (User Decision Adopted):** MLOps 추적성 확보를 위해 사후 원인 분석 리포트 구조 영속화 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 40] 판정 엔진(T250_Trigger)의 상태 보존(Statefulness)에 따른 제어권 침범
* **이슈 검토 결과 (Issue Validation):** 판정 엔진이 이상 횟수 누적용 `_trialCounter` 등 상태 변수를 들고 있어 자체 미니 FSM 제어권까지 일부 맡게 되어 SRP 위반.
* **개선 방안 (Proposed Resolution):** 판정 엔진은 순수 입력에만 의존하는 무상태(Stateless) 함수형 인터페이스로 축소하고, Trial 카운팅과 지연 판정 판단은 FSM 내 진단 코디네이터가 전담 수행.
* **사용자 결정 완료 사항 (User Decision Adopted):** 판정 엔진의 무상태화 및 책임 분리 설계 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 41] 비동기 스토리지(T260_Storage)의 프리트리거 덤프 시 파이프라인 락업(Lock-up)
* **이슈 검토 결과 (Issue Validation):** 이상 발생 시 최대 10초 분량 프리트리거 버퍼를 SD에 한 번에 덤프하면서 물리 SD MMC 쓰기 대역 병목으로 수집 루프가 일시 락업(Hang)됨.
* **개선 방안 (Proposed Resolution):** 대량 블로킹 덤프를 배제하고 Non-blocking 방식의 '점진적 청크 스트리밍 기입'으로 재설계해 핫루프 영역 보호.
* **사용자 결정 완료 사항 (User Decision Adopted):** 프리트리거 점진적 쓰기 링버퍼 기입 설계 확정.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 높음.

#### [리팩토링 대상 42] 캘리브레이터(T280_Calibrator)와 FSM 간 제어권 역전(Control Inversion) 현상
* **이슈 검토 결과 (Issue Validation):** 작업 수행자(T280)가 완료 시 FSM의 전역 커맨드 송출 함수를 직접 호출해 상태 전이를 유발하여 상하 역전 구조 및 결합도 상승.
* **개선 방안 (Proposed Resolution):** 전역 제어 호출을 박탈하고, 작업자는 완료 상태 플래그 세팅 및 이벤트 버스에 완료 메시지 송출만 수행하며 FSM 매니저 메인 루프에서 이를 감지해 상태 전이.
* **사용자 결정 완료 사항 (User Decision Adopted):** 단방향 이벤트 발행(Publish) 및 FSM 주도적 전이 구조 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

---

### 라. 4단계: 성능 및 무결성 교차 검증 (Performance & Logical Integrity Check)

#### [리팩토링 대상 43] PSRAM 캐시 일관성(Cache Coherence) 및 버스 대역폭 포화 검증
* **이슈 검토 결과 (Issue Validation):** Zero-Copy 링버퍼 도입 시, Core 0이 PSRAM 영역에 쓴 데이터가 캐시 불일치로 인해 Core 1에서 깨진 값(Data Tearing)으로 유입될 가능성 존재.
* **개선 방안 (Proposed Resolution):** PSRAM 접근 구간에 `Cache_WriteBack_Addr()` 및 `Cache_Invalidate_Addr()` 매크로를 명시하여 물리 메모리와 강제 동기화하고 스토리지 기입 트래픽 셰이핑 적용.
* **사용자 결정 완료 사항 (User Decision Adopted):** 캐시 동기화 매크로 명시 호출 강제 정책 채택.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 44] 비상 차단 인터록(Interlock)의 제어 지연(Latency) 모순 검증
* **이슈 검토 결과 (Issue Validation):** 판정 결과를 FSM이나 이벤트 버스 큐를 통해 통지할 경우 RTOS 틱 지연이 겹쳐 비상 GPIO LOW 응답성 사양을 만족하지 못함.
* **개선 방안 (Proposed Resolution):** 비상 차단 만큼은 이벤트 버스를 거치지 않고, 판정 엔진(T250) 내부에서 직접 GPIO 레지스터(`GPIO.out_w1tc`)를 직접 타격하는 Fast-Path 보장.
* **사용자 결정 완료 사항 (User Decision Adopted):** 안전 스펙 보장을 위해 예외적 Fast-Path 인터록 통로 유지.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 45] 범용 연산 루프 병합에 따른 Task WDT 기아 상태 검증
* **이슈 검토 결과 (Issue Validation):** 다채널 연산을 제네릭 루프로 통합 기동 시 Core 1 CPU 점유 시간이 길어져 IDLE 스레드 실행 기회가 박탈되어 Task Watchdog 패닉 유발 리스크 발생.
* **개선 방안 (Proposed Resolution):** 채널 순회 루프 전환 구간에 `taskYIELD()` 또는 마이크로초 단위 `vTaskDelay` 블로킹을 주어 스케줄러가 WDT Feeding을 처리할 기회 보장.
* **사용자 결정 완료 사항 (User Decision Adopted):** 핫루프 내 명시적 스케줄러 양보 지점 삽입 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 46] Mutex 우선순위 역전(Priority Inversion) 데드락 검증
* **이슈 검토 결과 (Issue Validation):** 설정, 스토리지, 보정이 서로 다른 우선순위 하에서 자원 락을 경합할 때 중간 우선순위 스레드가 선점하면 높은 우선순위 태스크가 영원히 대기하는 데드락 리스크 확인.
* **개선 방안 (Proposed Resolution):** 시스템의 모든 자원 세마포어 락을 FreeRTOS의 `xSemaphoreCreateMutex()` (우선순위 상속 메커니즘 포함)로 전면 교체 적용하고 RAII std::lock_guard 도입.
* **사용자 결정 완료 사항 (User Decision Adopted):** 우선순위 상속 Mutex 의무화 및 RAII 락 홀더 패턴 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 47] 부동소수점 예외(NaN/Inf) 페이로드 오염 및 파서 붕괴 검증
* **이슈 검토 결과 (Issue Validation):** IIR 필터 계산이나 나눗셈 연산(분모 0) 도중 NaN/Inf 발생 시, 필터링 없이 그대로 패킹되어 텔레메트리로 나가 웹 대시보드 파서를 붕괴시킴.
* **개선 방안 (Proposed Resolution):** 시퀀스 빌더(T248)에서 특징량을 바인딩 직전에 `std::isnan()`, `std::isinf()` 기반 검열 계층을 추가해 즉각 0.0f로 클램핑 처리.
* **사용자 결정 완료 사항 (User Decision Adopted):** 예외 수치 필터링 필터링 필터링 강제 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 낮음.

#### [리팩토링 대상 48] OTA 업데이트와 고속 스토리지 I/O 간의 버스 경합(Bus Contention) 검증
* **이슈 검토 결과 (Issue Validation):** OTA 플래시 기입 시점에 센서 덤프가 SD 카드로 밀어닥치면 SPI 버스 대역 포화로 OTA 타임아웃 펌웨어 브릭(벽돌) 위험 유발.
* **개선 방안 (Proposed Resolution):** OTA 진입 시 FSM 오케스트레이터가 즉각 스토리지 저장 및 센서 수집 작업을 Graceful Suspend 상태로 강제 전환.
* **사용자 결정 완료 사항 (User Decision Adopted):** OTA 시퀀스 내 타 작업 일시정지 및 차단 룰 적용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

#### [리팩토링 대상 49] 장기 운용 시스템 힙 파편화(Heap Fragmentation) 및 OOM(Out of Memory) 검증
* **이슈 검토 결과 (Issue Validation):** 설정 로드 및 MQTT 통신 중 String 객체 및 가변 힙 할당이 누적되어 장기 운용(수개월) 시 힙 파편화 OOM 크래시 초래.
* **개선 방안 (Proposed Resolution):** JSON 전역 할당을 ArduinoJson V7 Static 풀로 제약하고 런타임 String 객체를 전면 배제하여 고정 크기 `char` 배열과 `snprintf`로 변경 적용.
* **사용자 결정 완료 사항 (User Decision Adopted):** 런타임 동적 String 객체 사용 100% 금지 및 정적 문자 배열 활용.
* **구현 난이도 및 불가 여부 (Difficulty & Feasibility):** 보통.

---

## 4. 사용자 의사결정 필요 사항 종합 정리 (결정 완료)

본 리팩토링 및 개선 설계 진행에 있어 개발자 의사결정이 완료되어, 시스템에 적용하기로 확정된 세부 사양은 다음과 같습니다.


### 가. 메모리 및 버퍼 관리 정책
| 관련 이슈 | 의사결정 항목 | 확정된 설계 방향 (권장 설계 적용 완료) |
| :--- | :--- | :--- |
| **이슈 9** | 락프리 링버퍼 슬롯 생명주기 관리 방식 | **대안 2 (정적 배열 순환)**: 힙 단편화 방지 및 시스템 안정성을 위해 정적으로 할당된 인덱스 순환 방식 확정 적용. |
| **이슈 16** | 백그라운드 SD 기록 버퍼 용량 설정 | **3~5초 분량(약 500KB~1MB)**: SD_MMC Write/Flush의 최대 물리 레이턴시(500ms)를 커버하는 버퍼 공간을 안정 배정. |
| **이슈 21** | Pre-Trigger 버퍼 저장 범위 및 위치 | **3~5초 범위 제한 및 PSRAM 배치**: 힙 부족(OOM) 방지를 위해 PSRAM 영역을 사용하고 과거 저장 한계는 3~5초로 제한 확정. |
| **이슈 27** | RTC 보존 메모리 크기 배정 | **요약 진단 데이터 및 오류 상태 코드 (256바이트 미만)**: `RTC_DATA_ATTR` 영역을 극소화하여 L1 캐시 무결성 유지. |

### 나. 시스템 동작성 및 성능 트레이드오프 정책
| 관련 이슈 | 의사결정 항목 | 확정된 설계 방향 (권장 설계 적용 완료) |
| :--- | :--- | :--- |
| **이슈 10** | WAL 플래시 마모 최소화 기법 | **대안 2 (지연 쓰기 주기 3~5초 보강)**: 플래시 저널링 대신 Lazy Write 타이머를 보강하여 소거 횟수를 물리적으로 제어. |
| **이슈 20** | 핫스왑 중 데이터 수집 정책 | **대안 1 (일시중지)**: 네트워크 재연결 시의 스케줄러 병목을 피하기 위해 통신 데몬 기동 중 센서 파이프라인 정지. |
| **이슈 25** | closeSession 대기 타임아웃 | **3초 한계 설정**: 3초 이내에 링버퍼가 비워지지 않을 경우 예외 탈출 및 파일 셧다운 적용. |
| **이슈 30 / 36** | 텔레메트리 대역폭 및 드롭 지표 노출 | **UI 내 유실 카운트 통지**: 링버퍼 꽉 참으로 인한 프레임 드롭 발생 시 사용자 알람 표출 적용. |

### 다. 네트워크 및 MLOps 표준화 정책
| 관련 이슈 | 의사결정 항목 | 확정된 설계 방향 (권장 설계 적용 완료) |
| :--- | :--- | :--- |
| **이슈 37** | 폐쇄망(NTP 차단) 시 시스템 기동 정책 | **대안 2 (타임아웃 적용)**: 최대 30초 대기 후 동기 실패 시 1970년 가상 시각 기준으로 부팅 강행하여 동작 무결성 보장. |
| **이슈 39** | FATFS 예약 선할당 크기 | **세션당 10MB**: 일반 이벤트 파형 수집 크기 사양에 맞춰 파일 생성 시 디스크 10MB 영역 선할당 고정. |
| **이슈 43** | `ST_TriggerReason_t` MLOps 규격 | **바이너리 고정 크기 구조체**: JSON 직렬화 연산 오버헤드를 배제하기 위해 고정 바이너리 구조체 헤더 탑재. |
